using UnityEngine;

public class TemporaryDialogue : MonoBehaviour
{
    [SerializeField] private bool hasProperDialogue;

    // HELO YZA tbh I'm not sure if some of these should even contain dialogue
    // and it's a whole nother step to check the story docs so,,, imma leave this to u
    [SerializeField] private bool containsSFX;
}